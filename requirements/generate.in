# generate / validate runtime.
#
# This is the dependency set a *target* installs. Four pure-Python libraries plus a
# WebSocket client — no compiled extensions, no ML stack, nothing that pulls hundreds of
# transitive packages nobody can review. V2_FULL_PLAN.md §5.3 makes the boundary a hard
# one, and CI asserts torch is not importable here.
#
# Edit this file, then run scripts/lock.sh. Never edit generate.txt by hand.
httpx>=0.28.1
jsonpath-ng>=1.8.0
pydantic>=2.13.4
PyYAML>=6.0.3
websockets>=17.0.1
